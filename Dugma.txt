for every tile from 158* to 0:
//check if black:

1	if not black, look at next tile (next iteration) (->).

//check if on left border:

2	if on left border, check right move tile (->).

//check left move tile:

3	if left move tile has black, check right border (->).
4	if left move tile has white, check left leap (->). 
5	numOfMoves++
6	check right border (->). //write function here?	

//check left leap:
	
7	if left move tile is on last row, check right border (->).
8	if left move tile is on left border, check right border (->).
9	if left *leap* tile is not empty (=black or white, not 0), check right border (->).
10	numOfMoves++
11	check right border (->).


//check if on right border:

12	if on right border, look at next iteration (->).

//check right move tile:

13	if right move tile has black, look at next iteration (->).
14	if right move tile has white, check right leap (->). 
15	numOfMoves++
16	look at next iteration (->). //write function here?

	

//check right leap:
	
17	if right move tile is on last row, look at next iteration (->).
18	if right move tile is on right border, look at next iteration (->).
19	if right *leap* tile is not empty (=black or white, not 0), look at next iteration (->).
20	numOfMoves++
21	look at next iteration (->).

//next iteration:
	
	*just next iteration things*
	go to start of loop (->).


	
	